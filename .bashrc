# ~/.bashrc

# Enable color support for ls and grep
alias ls='ls --color=auto'
alias grep='grep --color=auto'

# Colored prompt: blue username@hostname, green current directory
PS1='\[\e[1;34m\]\u@\h \[\e[0;32m\]\w \$\[\e[0m\] '

